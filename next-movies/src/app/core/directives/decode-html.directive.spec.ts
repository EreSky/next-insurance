import { DecodeHtmlDirective } from './decode-html.directive';

describe('DecodeHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new DecodeHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
